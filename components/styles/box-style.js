import { StyleSheet } from 'react-native'

export const box_style = StyleSheet.create({
  box: {
    position: 'absolute',
    backgroundColor: "transparent",
    borderWidth: 1,
    borderColor: 'red',
    borderRadius: 4,
  },
  name: {
    position: 'absolute',
    color: "red",
    fontSize: 12,
    height: 50,
    width: 300

  }
})
